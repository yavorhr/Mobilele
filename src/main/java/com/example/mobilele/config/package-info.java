package com.example.mobilele.config;

/**
 * config package consists of ApplicationBeanConfig class.
 *
 * The class is annotated with @Configuration and @Component
 * It is used to create @Beans, which will be added in the Spring context container (IoC container).
 */
