package com.example.mobilele.init;

/**
 * init project contains DBInit class.
 *
 * The class implements CommandLineRunner interface
 * DBInit calls the service classes, which all have init() methods
 * for creating instances and persisting data in the database.
 *
 * The class is used for demo tests, until all features of the application are developed.
 */
